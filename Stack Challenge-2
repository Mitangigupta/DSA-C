#include<stdio.h>
int MAX;
int stack[100],top = -1;
void push(int value) {
    if (top == MAX - 1) {
        printf("Overflow\n");
    } else {
        top++;
        stack[top] = value;
    }
}
void pop() {
    if (top == -1) {
        printf("Underflow\n");
    } else {
        printf("Popped: %d\n", stack[top]);
        top--;
    }
}
void display() {
    if (top == -1) {
        printf("Stack is Empty\n");
    } else {

        for (int i = top; i>= 0; i--) {
            printf("%d ", stack[i]);
        }
    }
}

int main() {
    int i, value,n;
     
    scanf("%d", &MAX);
    scanf("%d",&n);
    for (i = 0; i < n; i++) {
        scanf("%d", &value);
        push(value);
    }
    for (i = 0; i < 2; i++) {
        pop();
    }
    for (i = 0; i < 4; i++) {
        scanf("%d", &value);
        push(value);
    }
    pop();
    for (i = 0; i < 2; i++) {
        scanf("%d", &value);
        push(value);
    }
     for (i = 0; i < 3; i++) {
        pop();
    }
    display();

    return 0;
}
